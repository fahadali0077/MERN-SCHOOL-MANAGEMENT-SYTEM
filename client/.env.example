# ─── API URL ──────────────────────────────────────────────────────────────────
# Leave empty in local development (Vite proxy handles /api → localhost:5000).
# Set this in Vercel dashboard → Settings → Environment Variables for production.
# Value = your Render backend URL WITHOUT trailing slash.
VITE_API_URL=https://schoolms-api.onrender.com
